name: Atividade Modulo 4

on:
   push:
      branches: [ "main" ]
   
   pull_request:
      branches: [ "main" ]

   schedule:
      - cron: '30 1 * * 5 '  # executa toda sexta 01:30

jobs:
    
    build:
     - name: Run a one-line script
        run: ECHO HELLO, WORLD!
        
